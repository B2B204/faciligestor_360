import { createEntity } from '@/lib/entity';
const _entity = createEntity('patrimony_depreciation_entries', { hasCreatedBy: false, hasUpdatedBy: false });
export const PatrimonyDepreciationEntry = _entity;
